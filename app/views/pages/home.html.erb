<h1>sesh</h1>

<%= form_tag instruments_path, method: :get do %>
  <%= select_tag :city, options_for_select(Instrument.distinct.pluck(:city)) %>
  <%= submit_tag "Search" %>
<% end %>

<% if user_signed_in? %>
  <%= link_to "My Dashboard", dashboard_path %>
<% end %>
